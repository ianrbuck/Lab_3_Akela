In palindrome.c we freed rev just before returning, which solved half of the leaks.
In palindrome_test.c we freed the string that palindrome outputs after it was called, which solved the other half of the leaks.
